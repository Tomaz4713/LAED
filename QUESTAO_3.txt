#QUESTÃO 03

#LETRA A
#Pseudocódigo:
#procedimento InverterFila(F):
#    P = CriarPilha()
#    enquanto FilaNaoVazia(F) faca:
#        x = Dequeue(F)
#        Push(P, x)
#    enquanto PilhaNaoVazia(P) faca:
#        x = Pop(P)
#        Enqueue(F, x)
#
#Rastreamento para a fila inicial F = <1, 2, 3, 4> (onde 1 está no início):
#1. Dequeue 1 -> Push(P, 1) | F = <2, 3, 4>,      P = [1]
#2. Dequeue 2 -> Push(P, 2) | F = <3, 4>,         P = [2, 1]
#3. Dequeue 3 -> Push(P, 3) | F = <4>,            P = [3, 2, 1]
#4. Dequeue 4 -> Push(P, 4) | F = <>,             P = [4, 3, 2, 1] (topo = 4)
#5. Pop 4 -> Enqueue(F, 4)  | F = <4>,            P = [3, 2, 1]
#6. Pop 3 -> Enqueue(F, 3)  | F = <4, 3>,         P = [2, 1]
#7. Pop 2 -> Enqueue(F, 2)  | F = <4, 3, 2>,      P = [1]
#8. Pop 1 -> Enqueue(F, 1)  | F = <4, 3, 2, 1>,   P = []
#Resultado final: F = <4, 3, 2, 1>

#LETRA B
#Tempo: O algoritmo realiza n operações de remoção da fila seguidas por n empilhamentos, e depois n desempilhamentos com n inserções na fila. Como cada operação individual custa O(1), o tempo total de execução é linear de O(n).
#Espaço: A pilha auxiliar armazena todos os n elementos transferidos da fila simultaneamente, consumindo um espaço adicional linear de O(n).

#LETRA C
#Sim, é possível inverter a fila in-place sem estruturas auxiliares. Como uma fila encadeada internamente é uma lista simplesmente encadeada delimitada pelos ponteiros inicio e fim, podemos percorrer os nós invertendo o sentido de seus ponteiros prox com três ponteiros auxiliares (ant, atual, prox_no) em tempo O(n) e espaço O(1), e ao final apenas trocar as referências dos ponteiros inicio e fim da fila.